services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: reviewflow
      POSTGRES_USER: reviewflow
      POSTGRES_PASSWORD: local_dev_only
    ports: ["5432:5432"]
  redis:
    image: redis:7
    ports: ["6379:6379"]
